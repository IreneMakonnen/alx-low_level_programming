#include <stdio.h>
/**
 * main - Print the alphabet in lowercase
 * Return: Always 0 (Success)
*/

int main(void)

{
	int ch = 'a';

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
			putchar('\n');
	return (0);
}
