#include <stdio.h>
#include "main.h"

/**
 * main - Print program name
 * @argc: Number of Arguments
 * @argv: Arrar of arguments
 * Return: 0 (Success)
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
