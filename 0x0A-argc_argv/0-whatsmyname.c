#include <stdio.h>
#include "main.h"

/**
 * main - Print program name
 * @argv: Number of Arguments
 * @argv: Arrar of arguments
 * Return: 0 (Success)
 */

int main(int argv, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
